import { urls, getJson } from './api.js';

const contentDiv = document.getElementById('content');

function start() {
  // Load the data using the provided API and display them in the content div in
  // order.
}

document.getElementById('startBtn').addEventListener('click', () => {
  start();
});
